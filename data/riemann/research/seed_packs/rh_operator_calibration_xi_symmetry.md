# Completed Xi Symmetry Calibration

This calibration seed isolates the completed-zeta and xi-function symmetry as a
standalone operator-language target.

Key objects:

- completed zeta
- xi-function `XI(s)`
- entire-function structure
- symmetry under `s -> 1-s`
- zero-location constraints stated in xi-function language

Calibration focus:

1. encode the completed xi-function as a canonical analytic object,
2. normalize the involution `XI(s) = XI(1-s)`,
3. preserve the fact that xi-function symmetry is necessary context but not a
   proof of RH by itself.

This seed should classify into the xi-function family and give the orchestrator
a direct source for completed-zeta symmetry instead of inferring it from
secondary narrative reports.
